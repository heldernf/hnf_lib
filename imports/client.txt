-- todo func para deixar executar uma função apenas quando a primeira chamada dela tiver sido
-- finalizada

-- function hnf.createCamera(coords, transitionTime)
--     local camera = CreateCam("DEFAULT_SCRIPTED_CAMERA", true)
--     SetCamCoord(camera, coords)
--     RenderScriptCams(true, not not transitionTime, transitionTime, true, true)
--     return camera
-- end

-- function hnf.destroyCamera(camera, transitionTime)
--     RenderScriptCams(false, not not transitionTime, transitionTime, true, true)
--     DestroyCam(camera, false)
-- end


